import { getColorScheme  } from "../components/Colors";
const colors = getColorScheme()
const FooterStyles = {
  container: {
    height: '100%',  
    width: '100%',
  },
  Icon: {
  },
  MapIconStyle:{
    height:60,
    width:60,
  }
};

export default FooterStyles;
